import React from "react";
import styled from "styled-components";

const MainSection = styled.section`
  width: 80%;
  margin: 32px auto;
`

const Main = ({ labels }) => {
  return (
    <MainSection>
      
    </MainSection>
  );
};

export default Main;